import 'package:flutter/material.dart';

class UsageData extends StatelessWidget {
  const UsageData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
